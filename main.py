


def main():
    while(1<2):#while app is killed
        #listen to the user
        #prompt cleanup and builder
        #request for inference
        #response cleanup and builder(identify if command execution or just response output)
        #execute command or output respnse(or both)



main()